# 🏠 Auto-Match Platform

Платформа автоматического сопоставления покупателей и продавцов недвижимости в Азербайджане.

## ✨ Возможности

### Основные функции
- **Telegram Bot** — полнофункциональный бот для покупателей и продавцов
- **REST API** — единый бэкенд для всех клиентов
- **Auto-Match Engine** — интеллектуальный алгоритм подбора совпадений
- **Анонимный чат** — безопасное общение между сторонами без раскрытия контактов
- **Геолокация** — поддержка PostGIS для поиска по районам и метро

### Premium функции
- **Подписки** — Premium, Agency Basic, Agency Pro
- **VIP-объявления** — приоритетное размещение на 7/14/30 дней
- **Агентские пакеты** — множественные VIP-слоты для агентств

### Админ-панель
- Веб-интерфейс по адресу `/admin`
- Статистика платформы в реальном времени
- Модерация объявлений (одобрение/отклонение)
- Управление пользователями (блокировка/разблокировка)
- Изменение статусов объявлений

## 🚀 Быстрый старт

### Требования
- Docker & Docker Compose
- Git

### Установка и запуск

```bash
# 1. Клонировать репозиторий
git clone https://github.com/meloch287/Auto-Match-Platform.git
cd Auto-Match-Platform

# 2. Создать файл конфигурации
cp .env.example .env

# 3. Отредактировать .env
# Обязательно указать:
# - BOT_TOKEN (получить у @BotFather)
# - DATABASE_URL
# - REDIS_URL

# 4. Запустить все сервисы
docker-compose up -d

# 5. Применить миграции базы данных
docker-compose exec api alembic upgrade head
```

### Доступ к сервисам

| Сервис | URL | Описание |
|--------|-----|----------|
| API | http://localhost:8000 | REST API |
| Swagger | http://localhost:8000/docs | Документация API |
| Админка | http://localhost:8000/admin | Веб-панель администратора |
| PostgreSQL | localhost:5432 | База данных |
| Redis | localhost:6379 | Кэш и очереди |

**Вход в админку:** логин `admin`, пароль `admin123`

## 📁 Структура проекта

```
auto-match-platform/
├── app/
│   ├── admin/              # Веб-админка (HTML/JS)
│   ├── api/                # FastAPI приложение
│   │   ├── v1/             # API endpoints версии 1
│   │   ├── deps.py         # Зависимости (auth, db)
│   │   ├── main.py         # Точка входа API
│   │   └── responses.py    # Форматы ответов
│   ├── bot/                # Telegram бот
│   │   ├── handlers/       # Обработчики команд
│   │   ├── keyboards/      # Клавиатуры и callbacks
│   │   ├── middlewares/    # Middleware (auth, i18n, throttling)
│   │   ├── translations/   # Переводы (az, ru, en)
│   │   ├── bot.py          # Инициализация бота
│   │   └── states.py       # FSM состояния
│   ├── core/               # Ядро приложения
│   │   ├── config.py       # Конфигурация
│   │   ├── database.py     # Подключение к БД
│   │   ├── security.py     # JWT, хеширование
│   │   └── validators.py   # Валидаторы данных
│   ├── models/             # SQLAlchemy модели
│   │   ├── user.py         # Пользователи
│   │   ├── listing.py      # Объявления
│   │   ├── requirement.py  # Заявки покупателей
│   │   ├── match.py        # Совпадения
│   │   ├── chat.py         # Чаты и сообщения
│   │   └── reference.py    # Справочники (локации, категории)
│   ├── repositories/       # Слой доступа к данным
│   ├── schemas/            # Pydantic схемы
│   ├── services/           # Бизнес-логика
│   │   ├── matching/       # Алгоритм Auto-Match
│   │   │   ├── engine.py   # Движок сопоставления
│   │   │   ├── scorer.py   # Расчёт score
│   │   │   └── duplicate.py # Детекция дубликатов
│   │   ├── subscription.py # Подписки
│   │   ├── notification.py # Уведомления
│   │   └── ...
│   └── workers/            # Фоновые задачи (arq)
├── alembic/                # Миграции базы данных
│   └── versions/           # Файлы миграций
├── tests/                  # Тесты
├── docker-compose.yml      # Docker конфигурация
├── Dockerfile              # Образ приложения
├── pyproject.toml          # Зависимости Python
└── .env.example            # Пример конфигурации
```

## 🛠 Технологический стек

| Компонент | Технология |
|-----------|------------|
| Backend | Python 3.11+, FastAPI |
| Bot | aiogram 3.x |
| Database | PostgreSQL 15 + PostGIS |
| Cache/Queue | Redis + arq |
| ORM | SQLAlchemy 2.0 (async) |
| Migrations | Alembic |
| Validation | Pydantic v2 |
| Auth | JWT (python-jose) |
| Container | Docker, Docker Compose |

## 🤖 Флоу Telegram бота

### Начало работы
```
/start → Выбор языка (🇦🇿 AZ / 🇷🇺 RU / 🇬🇧 EN) → Выбор роли (Покупатель / Продавец)
```

### Флоу покупателя (создание заявки)
1. **Категория** — Новостройка / Частный дом / Вторичка / Земля / Коммерция
2. **Локация** — Город/Район (до 5) или Метро или GPS
3. **Цена** — Диапазон min-max
4. **Оплата** — Наличные / Кредит / Оба варианта
5. **Первоначальный взнос** — Если кредит
6. **Комнаты** — Диапазон (или сотки для земли)
7. **Площадь** — Диапазон в м²
8. **Этаж** — Диапазон + предпочтения (не первый/не последний)
9. **Ремонт** — Множественный выбор
10. **Документы** — Тип документов
11. **Коммуникации** — Газ/Электричество/Вода
12. **Отопление** — Центральное/Индивидуальное/Комби
13. **Возраст** — Новый/Старый фонд
14. **Комментарий** — Дополнительные пожелания
15. **Подтверждение** → Auto-Match начинает поиск

### Флоу продавца (создание объявления)
1. **Категория** — Выбор типа недвижимости
2. **Локация** — Город/Район или Метро или GPS
3. **Цена** — Стоимость в AZN
4. **Оплата** — Наличные / Кредит / Оба
5. **Первоначальный взнос** — Если кредит
6. **Комнаты** — Количество (или сотки)
7. **Площадь** — В м²
8. **Этаж** — Этаж / Этажность здания
9. **Ремонт** — Состояние ремонта
10. **Документы** — Тип документов
11. **Коммуникации** — Газ/Электричество/Вода
12. **Отопление** — Тип отопления
13. **Возраст** — Новый/Старый фонд
14. **Фото** — 3-15 фотографий
15. **Видео** — Ссылка на YouTube/Vimeo (опционально)
16. **Описание** — Текст объявления
17. **Подтверждение** → Отправка на модерацию

## 🔧 Команды разработки

### Docker

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка
docker-compose down

# Пересборка с нуля
docker-compose down && docker-compose build --no-cache && docker-compose up -d

# Просмотр логов
docker logs automatch-api -f
docker logs automatch-bot -f
docker logs automatch-postgres -f

# Вход в контейнер
docker-compose exec api bash
docker-compose exec postgres psql -U postgres -d automatch
```

### База данных

```bash
# Применить все миграции
docker-compose exec api alembic upgrade head

# Откатить последнюю миграцию
docker-compose exec api alembic downgrade -1

# Создать новую миграцию
docker-compose exec api alembic revision --autogenerate -m "описание изменений"

# Посмотреть историю миграций
docker-compose exec api alembic history
```

### Тестирование

```bash
# Запуск всех тестов
pytest

# С покрытием кода
pytest --cov=app --cov-report=html

# Конкретный файл
pytest tests/test_validators_pbt.py

# Конкретный тест
pytest tests/test_matching_pbt.py::test_score_calculation -v
```

### Локальная разработка (без Docker)

```bash
# Создать виртуальное окружение
python -m venv venv
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac

# Установить зависимости
pip install -e ".[dev]"

# Запустить API
uvicorn app.api.main:app --reload --port 8000

# Запустить бота
python -m app.bot.main
```

## 🔐 Переменные окружения

```env
# Telegram Bot
BOT_TOKEN=your_bot_token_from_botfather

# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/automatch

# Redis
REDIS_URL=redis://redis:6379/0

# JWT
JWT_SECRET_KEY=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# App
DEBUG=true
APP_NAME=Auto-Match
API_PREFIX=/api/v1
```

## 📊 API Endpoints

### Аутентификация
- `POST /api/v1/auth/telegram` — Авторизация через Telegram
- `POST /api/v1/auth/refresh` — Обновление токена

### Пользователи
- `GET /api/v1/users/me` — Текущий пользователь
- `PATCH /api/v1/users/me` — Обновление профиля

### Объявления
- `GET /api/v1/listings` — Список объявлений
- `POST /api/v1/listings` — Создать объявление
- `GET /api/v1/listings/{id}` — Детали объявления
- `PATCH /api/v1/listings/{id}` — Обновить объявление

### Заявки
- `GET /api/v1/requirements` — Список заявок
- `POST /api/v1/requirements` — Создать заявку
- `GET /api/v1/requirements/{id}` — Детали заявки

### Совпадения
- `GET /api/v1/matches` — Список совпадений
- `POST /api/v1/matches/{id}/contact` — Инициировать контакт

### Чаты
- `GET /api/v1/chats` — Список чатов
- `GET /api/v1/chats/{id}/messages` — Сообщения чата
- `POST /api/v1/chats/{id}/messages` — Отправить сообщение

### Админка
- `POST /api/v1/admin/login` — Вход в админку
- `GET /api/v1/admin/stats` — Статистика
- `GET /api/v1/admin/listings` — Все объявления
- `POST /api/v1/admin/listings/{id}/status` — Изменить статус
- `GET /api/v1/admin/users` — Список пользователей
- `POST /api/v1/admin/users/{id}/block` — Заблокировать

## 📝 Лицензия

MIT
